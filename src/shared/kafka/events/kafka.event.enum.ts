export enum Topic {
    'SUBSCRIPTIONS_SUBSCRIPTION_CREATE' = 'subscriptions-subscription-create',
    'SUBSCRIPTIONS_SUBSCRIPTION_UPDATE' = 'subscriptions-subscription-update',
}

export enum EventEntityType {
    'SUBSCRIPTION' = 'SUBSCRIPTION',
}

export enum EventType {
    'CREATE' = 'CREATE',
    'UPDATE' = 'UPDATE',
    'DELETE' = 'DELETE',
}
